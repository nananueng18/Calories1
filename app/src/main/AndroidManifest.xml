<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mainueng.calorie.calories1">

    <application
        android:name=".Calories1"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Calories1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity><activity android:name=".ListActivity"/>
        <activity android:name=".RecordcalActivity" />
        <activity android:name=".EatActivity"/>
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".FoodSearch" />

        <meta-data
            android:name="AA_DB_NAME"
            android:value="Calories1.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <meta-data
            android:name="AA_MODELS"
            android:value="mainueng.calorie.calories1.Work,mainueng.calorie.calories1.Eat, mainueng.calorie.calories1.EatMeal" />


        <activity android:name=".HistoryActivity"></activity>
    </application>

</manifest>
